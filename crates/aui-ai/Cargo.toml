[package]
name = "aui-ai"
version = "0.1.0"
edition = "2024"

[dependencies]
gpui = "0.2.2"
reqwest = { version = "0.12", features = ["blocking", "json"] }
serde_json = "1.0"

[dev-dependencies]
tempfile = "3.24"
